import { BackgroundEl } from "../design/Hero";

const Hero = () => {
  return (
    <section className="relative">
      <div className="container mx-auto">
        <BackgroundEl />
      </div>
    </section>
  );
};

export default Hero;
